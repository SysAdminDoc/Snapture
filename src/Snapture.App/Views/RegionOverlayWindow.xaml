<Window x:Class="Snapture.App.Views.RegionOverlayWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Snapture Region"
        WindowStyle="None"
        AllowsTransparency="True"
        ResizeMode="NoResize"
        ShowInTaskbar="False"
        Topmost="True"
        Background="Transparent"
        Cursor="Cross"
        WindowStartupLocation="Manual"
        UseLayoutRounding="True"
        SnapsToDevicePixels="True"
        Foreground="{DynamicResource Text}"
        FontFamily="{StaticResource UiFont}"
        FontSize="13">
    <Window.Resources>
        <!--  Cached references so the overlay doesn't pull from app dictionaries every frame  -->
    </Window.Resources>

    <Grid x:Name="RootGrid">
        <!--  Image of frozen virtual screen; dimmed by overlay  -->
        <Image x:Name="BackgroundImage"
               Stretch="None"
               HorizontalAlignment="Left"
               VerticalAlignment="Top"
               SnapsToDevicePixels="True" />

        <!--  Dim layer using OpacityMask trick: full-screen dim, hole punched at selection  -->
        <Canvas x:Name="OverlayCanvas">
            <Rectangle x:Name="DimRect"
                       Width="0"
                       Height="0"
                       Fill="{DynamicResource DimBackdrop}" />
            <Rectangle x:Name="SelectionRect"
                       Stroke="{DynamicResource Accent}"
                       StrokeThickness="2"
                       Fill="{DynamicResource SelectionFill}"
                       Visibility="Collapsed"
                       SnapsToDevicePixels="True" />
            <Border x:Name="SizeBadge"
                    Background="{DynamicResource Mantle}"
                    BorderBrush="{DynamicResource Accent}"
                    BorderThickness="1"
                    CornerRadius="4"
                    Padding="6,3"
                    Visibility="Collapsed">
                <TextBlock x:Name="SizeText"
                           Foreground="{DynamicResource Text}"
                           FontSize="12"
                           FontFamily="Cascadia Code, Consolas, monospace" />
            </Border>
            <Border x:Name="LoupeBorder"
                    Width="120" Height="120"
                    Background="{DynamicResource Mantle}"
                    BorderBrush="{DynamicResource Accent}"
                    BorderThickness="2"
                    CornerRadius="60"
                    Visibility="Collapsed"
                    SnapsToDevicePixels="True">
                <Grid ClipToBounds="True">
                    <Image x:Name="LoupeImage" Stretch="None" />
                    <Path Stroke="{DynamicResource Accent}" StrokeThickness="1" Data="M 0,60 H 120 M 60,0 V 120" Opacity="0.7" />
                    <TextBlock x:Name="LoupeText"
                               Foreground="{DynamicResource Text}"
                               Background="{DynamicResource Mantle}"
                               FontFamily="Cascadia Code, Consolas, monospace"
                               FontSize="11"
                               Padding="4,1"
                               HorizontalAlignment="Center"
                               VerticalAlignment="Bottom"
                               Margin="0,0,0,4" />
                </Grid>
            </Border>
            <Border x:Name="HintBadge"
                    Background="{DynamicResource Mantle}"
                    BorderBrush="{DynamicResource Surface1}"
                    BorderThickness="1"
                    CornerRadius="6"
                    Padding="14,8"
                    Opacity="0.95"
                    HorizontalAlignment="Center">
                <TextBlock Foreground="{DynamicResource Subtext}" FontSize="13">
                    <Run Foreground="{DynamicResource Mauve}" Text="Drag" />
                    <Run Text=" to select region " />
                    <Run Foreground="{DynamicResource Surface2}" Text="·" />
                    <Run Foreground="{DynamicResource Mauve}" Text=" Esc " />
                    <Run Text="cancel" />
                    <Run Foreground="{DynamicResource Surface2}" Text=" · " />
                    <Run Foreground="{DynamicResource Mauve}" Text="Enter " />
                    <Run Text="confirm" />
                </TextBlock>
            </Border>
        </Canvas>
    </Grid>
</Window>
